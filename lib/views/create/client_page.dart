import 'package:flutter/material.dart';

class ClientPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Departamentos'),
      ),
      body: Center(
        child: Text('Página de Departamentos'),
      ),
    );
  }
}